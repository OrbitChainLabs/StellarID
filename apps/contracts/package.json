{
  "name": "@stellarid/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "StellarID Soroban smart contracts",
  "scripts": {
    "build": "cargo build --target wasm32-unknown-unknown --release",
    "test": "cargo test",
    "lint": "cargo clippy --all-targets --all-features -- -D warnings",
    "fmt": "cargo fmt",
    "fmt:check": "cargo fmt -- --check",
    "clean": "cargo clean"
  },
  "devDependencies": {}
}
